import React from 'react';

const AddRemove = () => {
    return (
        <div>
            <form>
                <input type="number" />
                <br />
                <button>Add</button>
                <button>Remove</button>
            </form>
        </div>
    );
};

export default AddRemove;